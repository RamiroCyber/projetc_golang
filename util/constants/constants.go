package constants

const (
	V1      = "v1"
	API     = "/api"
	VERSION = "Version"
)

// LOGGER PREFIX
const (
	Error   = "ERROR"
	Info    = "INFO"
	Warning = "WARNING"
)

// RESPONSE API ERRORS
const (
	InvalidUserID  = "Invalid user id"
	UserNotFound   = "User not found"
	InvalidAuth    = "Invalid email or password"
	InvalidPhone   = "Invalid phone number"
	FailedRegister = "Failed to register user"
)
